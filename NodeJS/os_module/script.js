const os = require("os");


//os.platform() which return the platform name ex. win32 or linux
// console.log(os.platform());

//os.arch() which return the architecture name ex. x64 or x32
// console.log(os.arch());

//os.cpus() which return the cpu information
// console.log(os.cpus());

//os.homedir() which return the home directory
// console.log(os.homedir());

//os.tempdir() which return the temporary directory
// console.log(os.tmpdir());


//os.userInfo() which return the user information
// console.log(os.userInfo())


//os.uptime() which return the system uptime
// console.log(os.uptime())


//os.networkInterfaces() which return the network interfaces
// console.log(os.networkInterfaces())


//os.release() which return the system release
// console.log(os.release())


//os.totalmem() which return the total memory
// console.log(os.totalmem())


//os.freemem() which return the free memory
// console.log(os.freemem())

//os.availableParallelism() which return the available parallelism ex. 4(cores) in my pc
// console.log(os.availableParallelism())

//os.hostname() which return the hostname ex. rahul
// console.log(os.hostname())